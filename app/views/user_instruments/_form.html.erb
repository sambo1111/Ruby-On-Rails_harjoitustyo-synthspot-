<%= form_for(@user_instrument) do |f| %>
  <% if @user_instrument.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_instrument.errors.count, "error") %> prohibited this user_instrument from being saved:</h2>

      <ul>
      <% @user_instrument.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :instrument_id %><br>
    <%= f.number_field :instrument_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
