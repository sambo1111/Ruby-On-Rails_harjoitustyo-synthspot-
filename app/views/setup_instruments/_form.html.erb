<%= form_for(@setup_instrument) do |f| %>
  <% if @setup_instrument.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@setup_instrument.errors.count, "error") %> prohibited this setup_instrument from being saved:</h2>

      <ul>
      <% @setup_instrument.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :instrument_setup_id %><br>
    <%= f.number_field :instrument_setup_id %>
  </div>
  <div class="field">
    <%= f.label :instrument_id %><br>
    <%= f.number_field :instrument_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
