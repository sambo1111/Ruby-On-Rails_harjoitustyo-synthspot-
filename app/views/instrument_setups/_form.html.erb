<%= form_for(@instrument_setup) do |f| %>
  <% if @instrument_setup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@instrument_setup.errors.count, "error") %> prohibited this instrument_setup from being saved:</h2>

      <ul>
      <% @instrument_setup.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :info %><br>
    <%= f.text_area :info %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
